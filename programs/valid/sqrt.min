// Declarations
var x:     float;
var guess: float;
var quot:  float;
var iter:  float;

// Statements
read x;
guess = 1;
iter  = 10;

while iter do
    quot  = x/guess;
    guess = 0.5 * (guess + quot);
    iter  = iter - 1;
done

print guess;
print guess * guess;
